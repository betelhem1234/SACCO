# Copy this file to .env and change the values in YOUR OWN environment.
# The real .env is never committed (see .gitignore).
#
# PostgreSQL superuser password for the sacco-db container.
# ALSO used by the api to connect (kept identical to avoid confusion).
POSTGRES_PASSWORD=password

# JWT signing secret. Use a long random string (e.g. `openssl rand -base64 64`).
# Tokens signed with a different secret will not validate after rotation.
APP_JWT_SECRET=vZ3xK8pT9mQ2sL6nR4wY7bC1dF5hJ0gAeXuIiOoPpAaSsDdFfGgHhJjKkLlZzXxCcVvBbNnMmQq